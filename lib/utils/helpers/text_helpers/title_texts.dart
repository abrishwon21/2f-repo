import 'package:flutter/material.dart';

headText(BuildContext context, color, fontSize, fontWeight) {
  return TextStyle(
    color: color,
    fontSize: fontSize,
    fontWeight: fontWeight,
  );
}
